enum HOME_PATH {
    ROOT = '/',
    PLAY_MEMU = '/play',
    LOGIN = '/login',
    SETTING = '/setting',
    ROOMS = '/rooms',
    LOBBY = '/lobby',
}

enum UNSUPPORTED_PATH {
    LOGGIN = '/login',
    SETTING = '/setting',
}

enum PATH {
    HOME_MENU = '/',
    PLAY_MEMU = '/play',
    ROOMS = '/rooms',
    LOBBY = '/lobby',
    GAME = '/game'
}

export { PATH, HOME_PATH, UNSUPPORTED_PATH };
